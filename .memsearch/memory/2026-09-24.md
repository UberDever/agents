## Project memory and boxed tools

- Decision (user): Use project-specific MemSearch collections selected by local
  `.memsearch.toml`; remove automatic commons search and promotion workflows.
- Decision (user): Clear all existing boxed indexed collections, preserving
  Markdown and configuration. Rebuild individual project indexes as needed.
- Decision (user): Host agents should prefer `~/dev/agents/pi-box/box` for approved
  tools, including MemSearch and tatr; consult the user for installation instructions.
- Decision (user): Keep ClickHouse work artifacts outside checkouts; apply this to
  Superpowers specs/plans and dialogue records. Ignore S3 organization work for now.
- [fact] (agent): Shared AGENTS.md now explicitly routes Superpowers artifacts and
  boxed tools. Codex instructions point to it; Pi mounts it read-only.
- Decision (user): tatr source is https://github.com/tsoding/tatr.
- [fact] (agent): tatr Dockerfile build is pinned to upstream commit
  `9b0d752d992c024875882937eb1a0705ba4ba7fb` and uses its documented nob build.
