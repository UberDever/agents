## Root-owned Praktika cleanup during devvm sync (13:38)

- [fact] (user+agent) Praktika Docker tests can leave root-owned `__pycache__`,
  `.pytest_cache`, and similar artifacts in VM task workspaces; host-user
  `git clean -ffdx` then fails with permission errors.
- Decision (user+agent): Normal `sync` first runs host-user cleanup. Only when
  that fails, it runs the identical Git clean with identical cache/submodule
  exclusions inside the existing `clickhouse/binary-builder` container, then
  reruns host cleanup as verification.
- Rejected (user+agent): Ignoring cleanup errors, because stale test artifacts
  violate exact-sync semantics; always using Docker, because common-path cost is
  unnecessary; recursive `chown`, because it is broad, slow, and can touch
  shared hard-linked object packs.
- [fact] (agent) Real VM validation removed 151 root-owned integration-test
  artifacts, preserved the existing build cache, and completed sync at local
  HEAD `ca6ea275c7e365387f50c85cfb8d2d60607eaeda`.
- Open: Root-owned artifacts inside a direct submodule checkout remain handled
  only if observed; current evidence involved top-level integration-test paths.
