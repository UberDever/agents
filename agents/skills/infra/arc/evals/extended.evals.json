{"skill_name":"arc","evals":[{"id":1,"prompt":"Я в директории /repo/services/myservice/ внутри локального Arcadia checkout. Как посмотреть текущую ветку?","expected_output":"Использует arc info --json и извлекает поле branch, НЕ arc branch --show-current","files":[],"expectations":["Использует команду arc info --json для получения текущей ветки","НЕ использует arc branch --show-current или git branch --show-current","Извлекает поле branch из JSON-вывода"]},{"id":3,"prompt":"Покажи последние 5 коммитов в моей ветке","expected_output":"Использует arc log -n 5, НЕ arc log -5","files":[],"expectations":["Использует arc log -n 5 (полная форма флага)","НЕ использует arc log -5 (сокращённый -N не поддерживается)"]},{"id":5,"prompt":"Мне прислали полное имя серверной ветки users/<username>/TICKET-1234. Переключись на неё.","expected_output":"Сразу планирует arc checkout users/<username>/TICKET-1234; не подменяет точное имя локальным TICKET-1234 и не ищет через arc branch -a | grep","files":[],"expectations":["Для указанной уже-префиксованной серверной ветки использует arc checkout users/<username>/TICKET-1234","НЕ подменяет точное имя командой arc checkout TICKET-1234 без префикса","НЕ начинает с arc branch -a для поиска ветки"]},{"id":6,"prompt":"Я работаю в локальном Arcadia checkout. Покажи мои открытые PR.","expected_output":"Использует arc pr list -o, НЕ arc pr list --mine","files":[],"expectations":["Использует arc pr list -o (или arc pr list, т.к. -o это дефолт)","НЕ использует arc pr list --mine (флаг не существует)"]},{"id":7,"prompt":"Создай PR с описанием ровно 'Add caching layer for user sessions', без автоматически добавленных сообщений коммитов. Отправь сразу на ревью.","expected_output":"Использует arc pr create --publish --no-commits -m '...' без отдельного arc push","files":[],"expectations":["Передаёт описание через флаг -m или --message (НЕ открывает интерактивный редактор)","Создаёт PR через arc pr create","Использует --publish для отправки на ревью","Передаёт --no-commits вместе с -m, чтобы commit message не задублировал описание в теле PR","НЕ использует несуществующую комбинацию arc submit --no-commits","Команда не требует интерактивного ввода"]},{"id":10,"prompt":"Я на обычной локальной ветке TICKET-1010 без префикса users/. Изменил src/push.go и src/push_test.go. Запуши эти изменения, PR создавать не нужно.","expected_output":"arc status, arc add src/push.go src/push_test.go, arc commit -m, голый arc push без -u","files":[],"expectations":["Проверяет состояние перед коммитом/пушем через arc status","Использует arc add src/push.go src/push_test.go (НЕ arc add .)","Делает arc commit -m с сообщением (НЕ без -m)","Для указанной unprefixed ветки делает голый arc push без -u и без явного users/<login>/... имени"]},{"id":14,"prompt":"Я на ветке users/<username>/TICKET-1234 (с префиксом). Запуши.","expected_output":"arc push -u users/<username>/TICKET-1234 с явным именем (fallback для prefixed ветки)","files":[],"expectations":["Делает arc push -u users/<username>/TICKET-1234 с явным именем ветки","НЕ делает голый arc push (вызовет двойной prefix users/<login>/users/<username>/TICKET-1234)","Объясняет почему нужен -u с явным именем для prefixed ветки"]},{"id":16,"prompt":"Я на ветке users/<username>/TICKET-1234. Сделай rebase на trunk и запушь.","expected_output":"arc rebase trunk + arc push -f -u users/<username>/TICKET-1234 (явное имя, т.к. ветка уже с префиксом)","files":[],"expectations":["Делает arc rebase trunk","Пушит с force через arc push -f -u users/<username>/TICKET-1234 (явное имя уже-префиксованной ветки)","Для указанной уже-префиксованной ветки НЕ делает и НЕ рекомендует голый arc push -f / arc push --force (двойной prefix). Допустимо отдельно упомянуть голый arc push только для явно проверенной непрефиксованной локальной ветки"],"covers":"scenario: rebase and sync (SKILL.md), force push on already-prefixed branch (double-prefix)"},{"id":18,"prompt":"Покажи только коммиты моей ветки, без коммитов из trunk","expected_output":"Использует arc log trunk..HEAD, НЕ arc log HEAD --not trunk","files":[],"expectations":["Использует arc log trunk..HEAD (range нотация работает в arc log)","НЕ использует arc log HEAD --not trunk (git-синтаксис, не поддерживается)","НЕ использует arc log -5 (сокращённая форма)"],"covers":"gotcha: log excluding trunk commits (SKILL.md line 108)"},{"id":19,"prompt":"Как посмотреть историю коммитов компактно: по одному хешу и сообщению на строку?","expected_output":"Использует arc log --oneline (или arc-нативный --format=\"{commit.short} {title}\"), НЕ git-style --format с %H/%s","files":[],"expectations":["Использует arc log --oneline (предпочтительно) или arc-нативный формат arc log --format=\"{commit.short} {title}\"","НЕ рекомендует git-style arc log --format с %H/%s как рабочую команду; допустимо предупредить, что в arc она печатает литеральную строку"],"covers":"gotcha: custom log format (SKILL.md Gotchas — arc-style --format allowed, git-style %H/%s not)"},{"id":22,"prompt":"Создай PR с описанием 'Enable automatic merge' и настрой автоматический мерж после прохождения всех проверок.","expected_output":"Использует arc pr create --publish --merge --no-commits -m, флаг --merge только в references","files":[],"expectations":["Использует arc pr create с флагом --merge (auto-merge после проверок)","Использует --publish для публикации","Передаёт описание через -m (НЕ без -m)","Передаёт --no-commits вместе с -m, чтобы commit message не задублировал описание в теле PR"],"covers":"reference-routed: PR flags (references/arc-commands.md line 107)"},{"id":23,"prompt":"Я работаю в локальном Arcadia checkout. Покажи только имена файлов, изменённых в моей ветке относительно trunk, без содержимого.","expected_output":"Использует arc diff -B --name-only","files":[],"expectations":["Использует arc diff -B (merge-base, изменения ветки)","Использует --name-only для вывода только имён файлов","НЕ использует arc diff trunk --name-only (может зависнуть)"],"covers":"reference-routed: diff flags (references/diff-review.md line 36)"},{"id":24,"prompt":"Я нахожусь в локальном Arcadia checkout. PR 55123 огромный, больше 5000 строк. Мне нужно получить список всех изменённых файлов, чтобы понять масштаб.","expected_output":"Использует arc pr changes 55123 с фильтрацией заголовков diff --git для извлечения списка изменённых файлов","files":[],"expectations":["Извлекает список изменённых файлов через arc pr changes 55123 с фильтрацией строк diff --git","НЕ использует arc pr view (открывает браузер)"],"covers":"reference-routed: file list extraction (references/pr-review.md line 20)"},{"id":26,"prompt":"Нужно удалить уже замерженную удалённую ветку users/<username>/TICKET-old. Как это сделать?","expected_output":"Использует arc push -d users/<username>/TICKET-old для удалённой ветки","files":[],"expectations":["Удаляет указанную удалённую ветку через arc push -d users/<username>/TICKET-old","НЕ использует arc branch users/<username>/TICKET-old без флага (создаст ветку!)","НЕ использует git branch -d"],"covers":"reference-routed: branch management (references/switch-branches.md), local -d and/or remote push -d"},{"id":29,"prompt":"Я уже делал arc submit раньше, сейчас на ветке pr-12345. Внёс ещё изменения, нужно обновить PR.","expected_output":"Использует arc submit без -m для обновления существующего PR","files":[],"expectations":["Использует arc submit (без -m, т.к. PR уже существует)","НЕ создаёт новый PR","Объясняет что submit обновит итерацию существующего PR"],"covers":"scenario: submit changes, update flow"},{"id":30,"prompt":"arc submit выдал конфликты с транком. Что делать?","expected_output":"Объясняет: resolve files, arc add, arc submit снова. Или arc submit --abort.","files":[],"expectations":["Объясняет что нужно разрешить конфликты в файлах","Упоминает arc add для пометки разрешённых файлов","Упоминает повторный arc submit после разрешения","Упоминает arc submit --abort как альтернативу"],"covers":"reference-routed: submit conflict handling (references/submit-changes.md)"},{"id":31,"prompt":"Я создал один PR через arc submit, но хочу создать ещё один независимый PR из других файлов, не трогая первый.","expected_output":"Использует arc submit --new -m для создания независимого PR","files":[],"expectations":["Использует arc submit --new -m для создания нового независимого PR","НЕ использует arc submit --stack вместо --new (stack предназначен для зависимых PR)","Объясняет что --new создаёт PR от той же точки транка"],"covers":"reference-routed: submit --new (references/submit-changes.md)"},{"id":34,"prompt":"Верни файл utils/helper.go к состоянию из trunk","expected_output":"Использует arc checkout trunk utils/helper.go (без --)","files":[],"expectations":["Использует arc checkout trunk utils/helper.go (без -- разделителя)","Использует двухаргументную форму checkout (commit + path)","НЕ использует -- разделитель (arc интерпретирует его как путь к файлу)"],"covers":"scenario: revert and undo, restore from commit"},{"id":35,"prompt":"Кто последний менял файл utils/auth.go? Покажи авторство построчно.","expected_output":"Использует arc blame utils/auth.go","files":[],"expectations":["Использует arc blame utils/auth.go для построчного авторства","НЕ использует git blame"],"covers":"scenario: blame and investigate (SKILL.md)"},{"id":36,"prompt":"В локальном Arcadia checkout найди, в каких коммитах добавлялась или удалялась строка parseToken.","expected_output":"Использует arc log -S parseToken","files":[],"expectations":["Использует arc log -S \"parseToken\" для поиска коммитов, добавлявших или удалявших строку","НЕ использует git log"],"covers":"scenario: blame and investigate, search by content"},{"id":38,"prompt":"При rebase на trunk хочу автоматически разрешить все конфликты в пользу изменений моей ветки.","expected_output":"Использует arc rebase trunk -X theirs","files":[],"expectations":["Использует arc rebase trunk -X theirs (или -X theirs с arc rebase)","Объясняет что при rebase 'theirs' означает изменения текущей ветки (контринтуитивно)"],"covers":"scenario: resolve conflicts, -X strategy"},{"id":40,"prompt":"Закрой PR 54321, он больше не нужен, мержить не надо","expected_output":"Использует arc pr discard 54321","files":[],"expectations":["Для закрытия PR рекомендует arc pr discard 54321","Не рекомендует и не выполняет gh pr close или arc pr close как команду закрытия; read-only проверка arc pr close --help не считается нарушением"],"covers":"reference: create-update-pr.md, close PR"},{"id":42,"prompt":"У меня ветка feature-B отведена от ветки feature-A. Хочу перебазировать feature-B напрямую на trunk.","expected_output":"Использует arc rebase --onto trunk feature-A feature-B","files":[],"expectations":["Использует полную команду arc rebase --onto trunk feature-A feature-B","Использует трёхаргументную форму rebase (--onto new-base old-base branch)"],"covers":"reference: rebase-sync.md, rebase --onto"},{"id":43,"prompt":"При cherry-pick возник конфликт, но этот коммит мне не критичен. Хочу его пропустить и продолжить.","expected_output":"Использует arc cherry-pick --skip","files":[],"expectations":["Для цели пользователя выбирает arc cherry-pick --skip и объясняет, что команда пропускает текущий коммит","Не рекомендует arc cherry-pick --abort для пропуска одного коммита; допустимо упомянуть abort только как отличающийся вариант, который отменяет всю операцию"],"covers":"reference: resolve-conflicts.md, --skip"},{"id":44,"prompt":"К ветке привязан PR. Нужно перегруппировать все коммиты, которые входят в PR начиная с точки расхождения с trunk, в логические группы. Ветка тысячи коммитов позади trunk, и сначала обновлять её до свежего trunk я не хочу.","expected_output":"Использует arc merge-base для safe reset, предупреждает про sparse-mount блокировку arc reset --soft trunk","files":[],"expectations":["Использует $(arc merge-base trunk HEAD) как точку отсчёта для перегруппировки (через arc reset --soft или arc rebase -i), а НЕ литеральный trunk","Предупреждает, что брать trunk напрямую как базу опасно на ветке, далеко ушедшей от trunk — приводит либо к sparse-mount ошибке 'Reset forbidden due to change outside the scope', либо к затаскиванию тысяч коммитов","НЕ предлагает arc reset --soft trunk как рабочий вариант"],"covers":"gotcha: sparse-mount reset to far trunk (SKILL.md Gotchas + reorganize-commits.md)"},{"id":45,"prompt":"После arc reset --soft до merge-base все изменения оказались в stage. Хочу снять со стейджа tools/old_debug.sh и tests/legacy_test.py, чтобы они не попали в первый коммит.","expected_output":"Использует arc reset HEAD <files> (двухпараметрическая форма), не голый arc reset","files":[],"expectations":["Использует arc reset HEAD tools/old_debug.sh tests/legacy_test.py для снятия выбранных файлов со stage","Объясняет что bare arc reset без аргументов = no-op (не git-style mixed reset)","НЕ советует голый arc reset как способ анстейджа"],"covers":"gotcha: bare arc reset is no-op (SKILL.md Gotchas)"},{"id":50,"prompt":"Нужно полностью разбить tracked файл include/structures.h на include/structure_A.h, include/structure_B.h и include/structure_C.h. Исходный файл больше не нужен. Важно сохранить историю строк в новых файлах.","expected_output":"Использует arc cp из исходника для каждого нового файла, редактирует копии, затем arc rm исходника","files":[],"expectations":["Делает три arc cp include/structures.h include/structure_X.h из исходного файла (НЕ цепочкой из уже отредактированной копии)","После arc cp редактирует каждую копию, оставляя только нужные строки","Удаляет исходник через arc rm include/structures.h после редактирования копий (full split)","Проверяет arc status --short и ожидает записи вида C include/structures.h -> include/structure_X.h, а также D include/structures.h для удалённого исходника"],"covers":"scenario: move, copy, or split tracked files — full split with arc rm (SKILL.md)"},{"id":52,"prompt":"В Arcadia отслеживаемый файл db/queries.py содержит user-related и order-related запросы. Хочу сохранить order-related запросы в db/queries.py, а user-related вынести в db/user_queries.py с сохранением истории строк.","expected_output":"Делает partial split через arc cp, оставляет исходный файл и разделяет содержимое","files":[],"expectations":["Перед редактированием делает один arc cp db/queries.py db/user_queries.py для сохранения истории","Оставляет user-related запросы в db/user_queries.py, удаляет их из db/queries.py и сохраняет order-related запросы в db/queries.py","Не удаляет db/queries.py через arc rm и не создаёт db/user_queries.py обычным cp, apply_patch Add File или копированием текста без arc cp"],"covers":"scenario: move, copy, or split tracked files — partial split with preserved history, SKILL.md"},{"id":53,"prompt":"Я создал PR 60123 через arc pr create -m без --no-commits, и теперь описание в PR задублировано — сообщение коммита повторяет то, что я уже написал в -m. Исходный текст -m: 'Add caching layer for user sessions'. Как это исправить, не пересоздавая PR?","expected_output":"Использует ya tool arcanum pr update-description --id 60123 --description 'Add caching layer for user sessions' для правки описания без пересоздания PR","files":[],"expectations":["Использует ya tool arcanum pr update-description с исходным текстом Add caching layer for user sessions без повторно вклеенного текста коммита","Передаёт id PR 60123 через флаг --id","НЕ использует legacy-инструмент gena-arcanum-cli","НЕ предлагает править описание командой arc: у arc pr нет режима редактирования","НЕ предлагает закрыть/удалить PR и создать новый"],"covers":"gotcha: commit messages auto-appended → duplicated description (references/create-update-pr.md, --no-commits)"},{"id":54,"prompt":"arc push сообщает, что серверная ветка должна быть сначала обновлена. Моя локальная ветка называется TICKET-1234 без префикса users/. Как безопасно продолжить?","expected_output":"Проверяет локальное имя через arc info --json, читает серверные изменения через arc pull и только затем при необходимости делает голый arc push -f","files":[],"expectations":["Проверяет фактическое локальное имя ветки через поле branch команды arc info --json, а не выводит имя из текста ошибки","Перед перезаписью серверной ветки выполняет arc pull и проверяет, нет ли нужных чужих коммитов","Для указанной непрефиксованной локальной ветки при подтверждённой необходимости использует голый arc push -f без -u и явного users/<login>/... имени","НЕ предлагает arc branch -u или arc push -f -u users/<login>/TICKET-1234 для указанной непрефиксованной локальной ветки"],"covers":"reference: push-changes.md, NoFastForward recovery without synthesized remote target"},{"id":56,"prompt":"Хочу вернуть изменения из последнего stash, но оставить сам stash в списке как резервную копию.","expected_output":"Использует arc stash apply, а не arc stash pop","files":[],"expectations":["Использует arc stash apply для применения последнего stash без удаления записи","НЕ использует arc stash pop, потому что он удаляет успешно применённый stash из списка","Объясняет, что stash остаётся доступным после apply"],"covers":"reference: stash-changes.md, apply versus pop"},{"id":57,"prompt":"У PR 42857 упала проверка. Подскажи, как посмотреть причину и оставить inline-комментарий к проблемной строке. Ничего не выполняй.","expected_output":"Передаёт работу со статусом CI, логами и review-комментарием Arcanum-скиллу","files":[],"expectations":["Для просмотра статуса и логов упавшей проверки использует arcanum-go","Для публикации inline review-комментария использует arcanum-go","НЕ пытается решить проверку CI и публикацию комментария только локальными командами arc"],"covers":"routing boundary: CI checks and review comments belong to Arcanum skill"},{"id":58,"prompt":"Создай черновой PR для текущей ветки. В описании надо объяснить, что мы убираем легаси-аргумент roslaunch_args, который перестал приниматься после рефакторинга и из-за которого операция падала с TypeError, что заодно снимаются три висячих PEERDIR, и что поднята версия операции с записью в CHANGELOG.","expected_output":"Первой строкой -m ставит короткий заголовок, объяснение выносит после пустой строки, после создания сверяет summary через arc pr status --json","files":[],"expectations":["Первая строка сообщения -m — короткий заголовок примерно до 100 символов, а НЕ абзац из нескольких предложений","Объяснение вынесено в тело после пустой строки, а не втиснуто в первую строку","Тело описания написано как самостоятельный текст, а не как продолжение фразы заголовка: первая строка в тело не попадает, summary и description не пересекаются","Создаёт черновой PR через arc pr create --publish=disabled --no-commits -m","После создания проверяет summary через arc pr status <pr-id> --json, а не считает достаточным напечатанный URL"],"covers":"gotcha: PR message title contract, summary/description disjoint (references/create-update-pr.md; SKILL.md Create or update PR)"},{"id":59,"prompt":"У PR 60500 заголовок получился на весь абзац и обрывается на полуслове — я передал в -m первой строкой три предложения. Тело описания при этом нормальное. Почини заголовок, не пересоздавая PR.","expected_output":"Использует ya tool arcanum pr update-summary --id 60500 --summary с коротким заголовком","files":[],"expectations":["Объясняет причину: первая строка -m целиком уходит в summary, а Арканум молча обрезает его примерно на 300 символах","Использует ya tool arcanum pr update-summary, передавая id 60500 через флаг --id","НЕ использует legacy-инструмент gena-arcanum-cli","Предлагает в качестве нового summary короткий заголовок, а не тот же абзац","НЕ предлагает чинить заголовок средствами arc: у arc pr нет режима редактирования","НЕ предлагает закрыть/удалить PR и создать новый"],"covers":"gotcha: silent ~300-character PR title truncation and recovery via pr update-summary (references/create-update-pr.md)"}]}